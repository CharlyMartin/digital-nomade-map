<div class="navbar-placeholder"></div>

<div class="form-container">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-sm-8 col-sm-offset-2 col-md-6 col-md-offset-3">

        <h2> <%= t('forms.edit_location') %> </h2>

        <%= simple_form_for [@nomad], url: nomad_path, method: :patch do |f| %>
          <div class="form-inputs">
            <%= f.input :latitude, label: false, placeholder: t('placeholders.first_name') %>
            <%= f.input :longitude, label: false, placeholder: t('placeholders.last_name') %>
          </div>
          <br>
          <div class="form-button">
            <%= f.submit t('forms.update'), class: 'primary-button' %>
          </div>
        <% end %>

      </div>
    </div>
  </div>
</div>
