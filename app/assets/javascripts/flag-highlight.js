// // 1. Selectors
// const locale = document.documentElement.lang;
// const flags = document.querySelectorAll(`img#${locale}`);


// // 2. Functions
// function setFlags() {
//   flags.forEach(flag => flag.setAttribute('id', 'flag-active'));
//   // ids are fr || en at pageload and then reset when the dom is fully loaded;
// };


// // 3. Event Listeners
// document.addEventListener('DOMContentLoaded', setFlags);
